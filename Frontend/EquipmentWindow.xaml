<Window x:Class="ServiceManagerApp.EquipmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServiceManagerApp"
        mc:Ignorable="d"
        Title="EquipmentWindow" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Vertical" Margin="0,10,0,0">
            <Button x:Name="btnOverView" Content="OverView" Width="100" Margin="4"/>
            <Button x:Name="btnTickets" Content="Tickets" Width="100" Margin="4"/>
            <Button x:Name="btnCustomers" Content="Customers" Width="100" Margin="4"/>
            <Button x:Name="btnEquipments" Content="Equipments" Width="100" Margin="4"/>
            <Button x:Name="btnParts" Content="Parts" Width="100" Margin="4"/>
            <Button x:Name="btnUsers" Content="Users" Width="100" Margin="4"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,10">
            <TextBox x:Name="txtCategory" Width="100" Margin="4"/>
            <TextBox x:Name="txtBrand" Width="100" Margin="4"/>
            <TextBox x:Name="txtModel" Width="100" Margin="4"/>
            <TextBox x:Name="txtSerialNumber" Width="100" Margin="4"/>
            <ComboBox x:Name="cmbCustomers" SelectedValuePath="Id" Width="250" Margin="5">
            <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Id, StringFormat='[#{0}] '}"/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button x:Name="btnAddEquipment" Content="Add Equipment" Width="100" Margin="4" Click="BtnAddEquipmentAsync"/>
        </StackPanel>

        <DataGrid x:Name="dataGridEquipments" Grid.Row="2" Grid.Column="1" Margin="5" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                <DataGridTextColumn Header="Brand" Binding="{Binding Brand}" Width="*"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="*"/>
                <DataGridTextColumn Header="Serial Number" Binding="{Binding SerialNumber}" Width="*"/>
                <DataGridTextColumn Header="Owner" Binding="{Binding CustomerId}" Width="*"/>

                <DataGridTemplateColumn Header="" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="X" Click="BtnDeleteEquipmentAsync"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>