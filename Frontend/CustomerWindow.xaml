<Window x:Class="ServiceManagerApp.CustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServiceManagerApp"
        mc:Ignorable="d"
        Title="CustomerWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Vertical" Margin="0,10,0,0">
            <Button x:Name="btnOverView" Content="OverView" Width="100" Margin="4"/>
            <Button x:Name="btnTickets" Content="Tickets" Width="100" Margin="4"/>
            <Button x:Name="btnCustomers" Content="Customers" Width="100" Margin="4"/>
            <Button x:Name="btnEquipments" Content="Equipments" Width="100" Margin="4"/>
            <Button x:Name="btnParts" Content="Parts" Width="100" Margin="4"/>
            <Button x:Name="btnUsers" Content="Users" Width="100" Margin="4"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,10">
            <TextBox x:Name="txtCustomerName" Width="100" Margin="4"/>
            <TextBox x:Name="txtCustomerPhone" Width="100" Margin="4"/>
            <TextBox x:Name="txtCustomerEmail" Width="100" Margin="4"/>
            <TextBox x:Name="txtCustomerAddress" Width="100" Margin="4"/>
            <Button x:Name="btnAddCustomer" Content="Add Customer" Width="100" Margin="4" Click="BtnAddCustomerAsync"/>
        </StackPanel>

        <DataGrid x:Name="dataGridCustomers" Grid.Row="2" Grid.Column="1" Margin="5" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
                <DataGridTextColumn Header="Created" Binding="{Binding Created}" Width="*"/>

                <DataGridTemplateColumn Header="" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="X" Click="BtnDeleteCustomerAsync"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>